interface:
  display_name: "AI Video Consistency"
  short_description: "Plan SD2-first AI animation continuity workflows"
  default_prompt: "Use $ai-video-consistency to turn a script into a structured SD2-first animation plan with shot breakdown, character and scene anchors, midpoint and tail frames, bridge-shot continuity, and repository memory when needed."
