interface:
  display_name: "Cloudflare Crawl Endpoint"
  short_description: "Plan and troubleshoot Cloudflare /crawl"
  default_prompt: "Use $crawl-endpoint to design a Cloudflare Browser Rendering /crawl job with the right filters, polling, and limits."
